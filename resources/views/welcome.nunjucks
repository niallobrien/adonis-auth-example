{% extends 'master' %}

{% block content %}
  <ul>
    <li>
      <a href="{{ 'signin' }}">Sign in</a>
    </li>
    <li>
      <a href="{{ 'signup' }}">Sign Up</a>
    </li>
  </ul>

  {{ form.open({action: 'IssuesController.store'}) }}
  {{ csrfField }}
  {{ form.input('text', 'name') }}
  {{ form.input('submit', 'submit') }}
  {{ form.close() }}
{% endblock %}
